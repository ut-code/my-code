---
id: python-basics-variable
title: 変数宣言と動的型付け
level: 2
question:
  - 変数の型を宣言しないことで、どのようなメリットとデメリットがありますか？
  - なぜ動的型付けが「奇妙」と感じられることがあるのですか？
  - '`type()`関数は、開発のどのような場面で役立つことがありますか？'
  - 一度作成した変数に異なる型のデータを再代入できると、コードが読みにくくなることはないのですか？
  - 変数名として使える文字や記号には何かルールがありますか？
---

## 変数宣言と動的型付け

Pythonの最も大きな特徴の一つは、変数の型を宣言する必要がないことです。変数への代入が、変数の作成と型の決定を同時に行います。

さらに、Pythonは**動的型付け**言語です。これは、一度作成した変数に、異なる型のデータを再代入できることを意味します。`type()`関数を使うと、変数が現在どの型を参照しているかを確認できます。

```python-repl
>>> # 変数 x を作成し、整数を代入
>>> x = 10
>>> print(x)
10
>>> type(x)
<class 'int'>
>>> # 同じ変数 x に文字列を再代入
>>> x = "Hello"
>>> print(x)
Hello
>>> type(x)
<class 'str'>
```

この柔軟性は、静的型付け言語に慣れていると少し奇妙に感じるかもしれませんが、Pythonの簡潔さと書きやすさの源泉となっています。
