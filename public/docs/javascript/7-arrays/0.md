---
id: javascript-arrays-0
title: 配列リテラルと基本的な操作
level: 2
---

## 配列リテラルと基本的な操作

JavaScriptの配列は`Array`オブジェクトですが、通常はリテラル `[]` を使用して生成します。
基本的な操作として、スタック操作（`push`, `pop`）やキュー操作に近いこと（`shift`, `unshift`）、そして万能な要素操作メソッド`splice`があります。
