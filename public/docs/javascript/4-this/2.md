---
id: javascript-this-2
title: 'パターンB: 関数呼び出し'
level: 3
---

### パターンB: 関数呼び出し

関数を単体で呼び出した場合（`func()`）、`this` は**グローバルオブジェクト**（ブラウザでは `window`、Node.jsでは `global`）になります。
ただし、**Strict Mode（`"use strict"`）**では、安全のため `undefined` になります。
