---
id: javascript-this-7-this
title: "3. アロー関数と 'this'"
level: 2
---

## 3\. アロー関数と 'this'

ES2015 (ES6) で導入されたアロー関数は、これまで説明したルールとは全く異なる挙動をします。

アロー関数には**独自の `this` が存在しません**。アロー関数内部の `this` は、**その関数が定義されたスコープ（レキシカルスコープ）の `this`** をそのまま参照します。

これは、「コールバック関数内で `this` が変わってしまう問題」を解決するのに最適です。
