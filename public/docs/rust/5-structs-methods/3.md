---
id: rust-structs-methods-3
title: 構造体更新記法
level: 3
---

### 構造体更新記法

既存のインスタンスの値を元に、一部だけ変更した新しいインスタンスを作成する場合、`..` 構文を使用できます。

```rust
// user1のデータを元に、emailだけ変更したuser2を作成
let user2 = User {
    email: String::from("another@example.com"),
    ..user1 // 残りのフィールドはuser1と同じ値が入る
};
```
