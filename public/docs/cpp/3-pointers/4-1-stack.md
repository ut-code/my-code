---
id: cpp-pointers-stack
title: スタック (Stack)
level: 3
question:
  - スタックのサイズ制限とは、具体的にどのくらいのサイズを指すのか。
  - 関数のスコープを抜けるとスタック上の変数が「自動的に消滅」するとは、メモリから完全に削除されるということか。
  - 再帰呼び出しを多用するとスタックオーバーフローが起きると聞いたことがあるが、それはスタックの性質とどう関係があるのか。
---

### スタック (Stack)

  * これまでの変数は主にここに置かれます。
  * 関数のスコープ `{ ... }` を抜けると**自動的に消滅**します。
  * 管理が楽で高速ですが、サイズに制限があります。
